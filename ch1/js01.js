// 声明式函数
function hello(name) {
  console.log(name);
}

// 函数表达式
let hello1 = function (name) {
  console.log(name);
};

// 箭头函数
let hello2 = (name) => {
  console.log(name);
};

// 使用无差别
hello("张三")
